<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MongoDB CRUD </title>
</head>
<body>
    <h1>MongoDB Crud operation</h1>
    {{message}}
    <br>
    {{#if edit_id}}

    <h3>Update book details</h3><br>
    <form method="post" action="/update_book/{{edit_id}}">
        <label for="title">Title</label>
        <input type="text" id="title" name="title" value="{{edit_book.title}}">&nbsp;
        <label for="author">Author</label>
        <input type="text" id="author" name="author" value="{{edit_book.author}}">
        <input type="Submit">
    </form><br>
    {{else}}
    <h3>Add book details</h3><br>
    <form method="post" action="/store_book">
        <label for="title">Title</label>
        <input type="text" id="title" name="title">&nbsp;
        <label for="author">Author</label>
        <input type="text" id="author" name="author">
        <input type="Submit">
    </form><br>

    {{/if}}
    
    <ul>
        {{#each books}}
        <li>{{this.title}}-{{this.author}} <a href="/?edit_id={{this._id}}">Edit</a> <a href="/?delete_id={{this._id}}" onclick="return confirm('Are you want to delete?')">Delete</a> </li>
        {{/each}}
    </ul>
    <br>
</body>
</html>